# Cloudflare Pages configuration
# Build command and output directory are configured in the
# Cloudflare Pages Dashboard, not here.

name = "moviezhou"
compatibility_date = "2026-07-09"
pages_build_output_dir = "dist"
